//プリティア
// コード保存時の整形ルールを定義
{
  "semi": true,          // 行末にセミコロン（;）を必ず付ける
  "singleQuote": true,   // 文字列はダブルクォーテーション（"）ではなくシングル（'）に統一する
  "tabWidth": 2,         // インデントの幅はスペース2個分
  "trailingComma": "all" // 配列やオブジェクトの末尾にカンマ（,）を自動で付ける
}
